name: eslint

on:
  pull_request:
    types: [opened, synchronize]
  push:
    branches: [main]

defaults:
  run:
    shell: bash
    working-directory: functions

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: npm install
        run: npm install
      - name: Run eslint
        run: npm run lint
